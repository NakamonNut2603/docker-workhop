pipeline {
    agent any
    stages {
        stage('Deploy to Kubernetes') {
            steps {
                sh 'echo "Deploy to Kubernetes"'
                sh 'kubectl apply -f helm/deployment.yaml -n jenkins-to-kube'
            }
        }
    }
    post {
        cleanup {
            sh 'echo "Clean up Workspace"'
            cleanWs()
        }
    }
}